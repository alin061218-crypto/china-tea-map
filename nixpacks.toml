# Railway deploy config
[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install --ignore-scripts && cd client && npm install && cd ../server && npm install"]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "cd server && node index.js"
